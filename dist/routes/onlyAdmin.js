import express from 'express';
const router = express.Router();
/**
 * FULL CRUD FOR ADMIN ONLY!
 */
//Create card
// router.post('/create-card', {authenticate admin role} , async(req,res));
//Update card
//delete card
export default router;
